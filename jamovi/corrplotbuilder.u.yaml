title: Correlation Matrix
name: corrplotbuilder
jus: '3.0'
stage: 0
children:
  - type: VariableSupplier
    persistentItems: false
    stretchFactor: 1
    children:
      - type: TargetLayoutBox
        label: Variables
        children:
          - type: VariablesListBox
            name: vars
            isTarget: true
  # Styled muted-italic by the shared gbPanelTip helper, which finds it
  # by the zero-width marker at the start of the label below. Keep the
  # marker when editing the wording; losing it only means the tip
  # renders plain.
  - type: Label
    margin: small
    label: '​Two or more numeric variables: different measures, not conditions.'
compilerMode: tame
